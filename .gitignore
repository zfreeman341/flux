# Rust build artifacts
/target

# Per-run state — generated at runtime, never committed
.flux-runs/

# Final outputs — personal content, never committed
outputs/

# Personal data — lives outside the repo, but exclude defensively
.flux-private/
flux-private/

# Environment files — API keys live here, never committed
.env
.env.*

# Logs
*.log

# macOS
.DS_Store

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code project instructions and session planning docs — personal context, not for public repo
Claude.MD
CLAUDE.md
0*.MD
